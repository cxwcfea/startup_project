<div class="am-g">
    <br>
    <div class="am-panel am-panel-default am-u-sm-12 am-u-md-8 am-u-md-centered">
        <div class="am-panel-hd">免费体验</div>
        <div class="am-panel-bd">
            <div class="apply am-g">
                <div class="money_je am-u-sm-10 pull-right"> 您交：<span class="am-icon-rmb"></span><span id="span_all">1</span>元</div>
            </div>
            <div class="apply am-g">
                <div class="money_je am-u-sm-10 pull-right"> 我们出：<span class="am-icon-rmb"></span><span id="span_all">2000</span>元</div>
            </div>
            <div class="apply am-g">
                <div class="money_je am-u-sm-10 pull-right"> 总共：<span class="am-icon-rmb"></span><span id="span_all">2001</span>元</div>
            </div>
            <div class="apply am-g">
                <div class="money_je am-u-sm-10 pull-right"> 交易：<span id="span_all">2</span>天</div>
            </div>
            <hr>
            <div class="apply am-g">
                {{#unless useBalance}}
                    <div class="money_je am-u-sm-10 pull-right"> 需支付：<span class="am-icon-rmb"></span><span id="span_all">1</span>元</div>
                {{else}}
                    <div class="money_je am-u-sm-10 pull-right"> 余额支付：<span class="am-icon-rmb"></span><span id="span_all">1</span>元</div>
                {{/unless}}
            </div>

            <form id="financingForm" name="financingForm">
                <input type="hidden" name="pay_amount" id="pay_amount" value="1">
                <input type="hidden" name="order_id" id="order_id" value="{{order._id}}">
                <input type="hidden" name="user_id" id="user_id" value="{{user._id}}">
                <input type="hidden" name="apply_id" id="apply_id" value="{{serialID}}">
                {{#if transID}}
                    <input type="hidden" name="trans_id" id="trans_id" value="{{transID}}">
                {{/if}}
            </form>

            <form action="https://mas.sdo.com/web-acquire-channel/cashier.htm" method="POST" id="shengPayForm" name="shengPayForm">
                <input type="hidden" name="Name" id="Name" value="B2CPayment">
                <input type="hidden" name="Version" id="Version" value="V4.1.1.1.1">
                <input type="hidden" name="Charset" id="Charset" value="UTF-8">
                <input type="hidden" name="MsgSender" id="MsgSender" value="100894">
                <input type="hidden" name="OrderNo" id="OrderNo" value="{{order._id}}">
                <input type="hidden" name="OrderAmount" id="OrderAmount" value="1">
                <input type="hidden" name="OrderTime" id="OrderTime" value="{{shengOrderTime}}">
                <input type="hidden" name="PageUrl" id="PageUrl" value="{{callback_domain}}/thank_you_for_pay">
                <input type="hidden" name="BackUrl" id="BackUrl" value="{{callback_domain}}/pay_confirm/{{order._id}}">
                <input type="hidden" name="NotifyUrl" id="NotifyUrl" value="{{callback_domain}}/api/shengpay_feedback">
                <input type="hidden" name="ProductName" id="ProductName" value="股票配资">
                <input type="hidden" name="BuyerIp" id="BuyerIp">
                <input type="hidden" name="SignType" id="SignType" value="MD5">
                <input type="hidden" name="SignMsg" id="SignMsg">
            </form>


            <div class="am-g">
                <div class="am-u-sm-6 pull-right">
                    {{#unless useBalance}}
                        <select id="pay-select" data-am-selected="{btnSize: 'sm'}">
                            <option value="option1">通道一:5000元以上使用</option>
                            <option value="option2">通道二:移动用户使用</option>
                        </select>
                        <span class="am-form-caret"></span>
                        <button id="go-to-pay" type="submit" class="am-btn am-btn-warning"> 确认支付 </button>
                    {{else}}
                        <button id="go-to-use-balance" type="submit" class="am-btn am-btn-warning"> 余额支付 </button>
                    {{/unless}}
                </div>
            </div>

            <div class="am-modal am-modal-confirm" tabindex="-1" id="pay-confirm">
                <div class="am-modal-dialog">
                    <div class="am-modal-hd">充值确认</div>
                    <div class="am-modal-bd">
                        <h2>请您在新打开的第三方支付页面上完成前请不要关闭此窗口</h2>
                        完成支付后请根据您的情况点击下面的按钮：
                    </div>
                    <div class="am-modal-footer">
                        <span class="am-modal-btn" data-am-modal-cancel>支付遇到问题</span>
                        <span class="am-modal-btn" data-am-modal-confirm>已完成支付</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>